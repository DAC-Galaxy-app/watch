# Samsung Health Sensor SDK

Place the Samsung Health Sensor SDK v1.4.1 `.aar` file in this directory.

The Gradle build includes `wear/libs/*.aar` automatically. The actual `.aar`
file is ignored by Git because Samsung SDK binaries should not be committed.
